version: "3"
services:
  pihole:
    container_name: pihole
    image: pihole/pihole:nightly
    ports:
      - "8080:80/tcp"
    environment:
      WEBPASSWORD: test
    cap_add:
      - NET_ADMIN
    restart: unless-stopped
