package pihole

import (
	"testing"
)

func testCreateDomains(DomainList) {

}

func testCleanupDomains(DomainList) {

}

func TestDomains(t *testing.T) {
	// t.Run("fetch list of allowed domains", func(t *testing.T) {
	// 	isAcceptance(t)

	// 	c := newTestClient()

	// 	domains, err := c.Domains.List(context.Background(), &DomainListOptions{
	// 		ListTypes: []string{"white"},
	// 	})
	// 	require.NoError(t, err)

	// 	assert.Len(t, domains, 1)
	// })
}
